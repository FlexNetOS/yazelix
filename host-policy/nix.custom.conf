# FlexNetOS/Yazelix cache policy.
# Signed binary substituters restored; Kache remains the local compilation cache
# (they are different layers — Kache caches build object files, substituters serve
# whole store paths). require-sigs stays true (default): only NixOS- and
# nix-community-signed store paths are ever trusted.

always-allow-substitutes = true
compress-build-log = false
keep-build-log = false
substitute = true
substituters = https://cache.nixos.org https://nix-community.cachix.org
trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs=
trusted-substituters =
trusted-users = root flexnetos
