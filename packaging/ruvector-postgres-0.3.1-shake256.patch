diff --git a/Cargo.lock b/Cargo.lock
index 3b4f84f..35af1fa 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -8929,6 +8929,7 @@ dependencies = [
  "ruvector-mincut-gated-transformer 0.1.0",
  "ruvector-solver",
  "ruvector-sona 0.1.6",
+ "rvf-crypto",
  "serde",
  "serde_json",
  "simsimd",
diff --git a/crates/ruvector-postgres/Cargo.toml b/crates/ruvector-postgres/Cargo.toml
index a9759bf..dd7035c 100644
--- a/crates/ruvector-postgres/Cargo.toml
+++ b/crates/ruvector-postgres/Cargo.toml
@@ -78,6 +78,9 @@ all-features-v3 = ["all-features", "analytics-complete", "ai-complete-v3", "doma
 # PostgreSQL extension framework
 pgrx = "0.12"
 
+# Canonical SHAKE256 implementation shared with RVF witness hashing.
+rvf-crypto = { path = "../rvf/rvf-crypto", default-features = false, features = ["std"] }
+
 # Pin home to avoid edition2024 issues
 home = "=0.5.9"
 
diff --git a/crates/ruvector-postgres/ruvector.control b/crates/ruvector-postgres/ruvector.control
index d08dc8c..37e2b98 100644
--- a/crates/ruvector-postgres/ruvector.control
+++ b/crates/ruvector-postgres/ruvector.control
@@ -3,8 +3,8 @@
 # Features: 230+ SQL functions, Flash Attention, GNN, hybrid search, multi-tenancy, self-healing
 
 comment = 'RuVector v0.3: SIMD-optimized vector similarity search with solver, math, TDA, and AI capabilities'
-default_version = '0.3.0'
+default_version = '0.3.1'
 module_pathname = '$libdir/ruvector'
-relocatable = false
-superuser = false
+relocatable = true
+superuser = true
 trusted = true
diff --git a/crates/ruvector-postgres/sql/ruvector--0.3.0--0.3.1.sql b/crates/ruvector-postgres/sql/ruvector--0.3.0--0.3.1.sql
new file mode 100644
index 0000000..c8112c5
--- /dev/null
+++ b/crates/ruvector-postgres/sql/ruvector--0.3.0--0.3.1.sql
@@ -0,0 +1,9 @@
+-- RuVector PostgreSQL Extension v0.3.1 Upgrade Script
+-- Adds the canonical RVF SHAKE256-256 digest primitive.
+
+\echo Use "ALTER EXTENSION ruvector UPDATE TO '0.3.1'" to load this file. \quit
+
+CREATE FUNCTION ruvector_shake256_256(input bytea)
+RETURNS bytea
+AS 'MODULE_PATHNAME', 'ruvector_shake256_256_wrapper'
+LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE;
diff --git a/crates/ruvector-postgres/src/bin/pgrx_embed.rs b/crates/ruvector-postgres/src/bin/pgrx_embed.rs
index c8d4433..1fb4c43 100644
--- a/crates/ruvector-postgres/src/bin/pgrx_embed.rs
+++ b/crates/ruvector-postgres/src/bin/pgrx_embed.rs
@@ -1,8 +1,5 @@
-//! pgrx SQL generator binary
-//!
-//! This binary is used by pgrx to generate SQL schema files.
+//! pgrx SQL entity-graph generator.
 
-fn main() {
-    // This is a placeholder binary for pgrx SQL generation.
-    // pgrx uses this to discover and generate SQL for the extension.
-}
+#![allow(unexpected_cfgs)]
+
+::pgrx::pgrx_embed!();
diff --git a/crates/ruvector-postgres/src/crypto.rs b/crates/ruvector-postgres/src/crypto.rs
new file mode 100644
index 0000000..0903549
--- /dev/null
+++ b/crates/ruvector-postgres/src/crypto.rs
@@ -0,0 +1,42 @@
+//! Cryptographic SQL functions backed by the canonical RVF primitives.
+
+use pgrx::prelude::*;
+
+/// Return the 256-bit SHAKE256 digest of an arbitrary PostgreSQL `bytea`.
+///
+/// `Vec<u8>` maps to `bytea` in pgrx. A non-optional argument also makes the
+/// generated SQL function `STRICT`, so PostgreSQL returns `NULL` without
+/// invoking Rust when the input is `NULL`.
+#[pg_extern(immutable, parallel_safe)]
+pub fn ruvector_shake256_256(input: Vec<u8>) -> Vec<u8> {
+    rvf_crypto::shake256_256(&input).to_vec()
+}
+
+#[cfg(test)]
+mod tests {
+    use super::ruvector_shake256_256;
+
+    #[test]
+    fn shake256_256_matches_nist_empty_input_vector() {
+        assert_eq!(
+            ruvector_shake256_256(Vec::new()),
+            [
+                0x46, 0xb9, 0xdd, 0x2b, 0x0b, 0xa8, 0x8d, 0x13, 0x23, 0x3b, 0x3f, 0xeb, 0x74, 0x3e,
+                0xeb, 0x24, 0x3f, 0xcd, 0x52, 0xea, 0x62, 0xb8, 0x1b, 0x82, 0xb5, 0x0c, 0x27, 0x64,
+                0x6e, 0xd5, 0x76, 0x2f,
+            ]
+        );
+    }
+
+    #[test]
+    fn shake256_256_matches_nist_abc_vector() {
+        assert_eq!(
+            ruvector_shake256_256(b"abc".to_vec()),
+            [
+                0x48, 0x33, 0x66, 0x60, 0x13, 0x60, 0xa8, 0x77, 0x1c, 0x68, 0x63, 0x08, 0x0c, 0xc4,
+                0x11, 0x4d, 0x8d, 0xb4, 0x45, 0x30, 0xf8, 0xf1, 0xe1, 0xee, 0x4f, 0x94, 0xea, 0x37,
+                0xe7, 0x8b, 0x57, 0x39,
+            ]
+        );
+    }
+}
diff --git a/crates/ruvector-postgres/src/lib.rs b/crates/ruvector-postgres/src/lib.rs
index 8bec35b..8530db4 100644
--- a/crates/ruvector-postgres/src/lib.rs
+++ b/crates/ruvector-postgres/src/lib.rs
@@ -21,6 +21,7 @@ use pgrx::{GucContext, GucFlags, GucRegistry, GucSetting};
 
 // Module declarations
 pub mod attention;
+pub mod crypto;
 pub mod dag;
 pub mod distance;
 pub mod gnn;
